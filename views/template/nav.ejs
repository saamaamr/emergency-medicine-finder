<div class="hero_area">
  <div class="hero_bg_box">
    <img src="images/hero-bg.jpg" alt="" />
  </div>
  <header class="header_section">
    <div class="container-fluid">
      <nav class="navbar navbar-expand-lg custom_nav-container">
        <a class="navbar-brand" href="/home">
          <img width="130" src="images/logo_main.png" alt="EMF" />
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
          aria-label="Toggle navigation">
          <i class="ri-menu-line"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item <%= typeof activeNav !== 'undefined' && activeNav === 'home' ? 'active' : '' %>">
              <a class="nav-link" href="/home">Home</a>
            </li>
            <li class="nav-item <%= typeof activeNav !== 'undefined' && activeNav === 'about' ? 'active' : '' %>">
              <a class="nav-link" href="/about">About</a>
            </li>
            <% if(typeof uId !== 'undefined' && uId) { %>
              <li class="nav-item">
                <a class="nav-link" href="/profile">
                  <i class="ri-user-3-line"></i> <%= typeof userData !== 'undefined' && userData && userData[0] ? userData[0].first_name : '' %>
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/userrequest">My Requests</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
              </li>
            <% } else { %>
              <li class="nav-item">
                <a class="nav-link" href="/login">User Login</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/shopkeeperlogin">Shopkeeper Login</a>
              </li>
            <% } %>
          </ul>
          <% if(typeof uId !== 'undefined' && uId) { %>
            <form class="d-flex" role="search" onsubmit="return false;">
              <input class="form-control me-2" id="searchmedi" type="search" placeholder="Search Medicine" aria-label="Search">
            </form>
          <% } %>
        </div>
      </nav>
    </div>
  </header>
</div>
